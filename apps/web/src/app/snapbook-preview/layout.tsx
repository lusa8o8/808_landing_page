import type { ReactNode } from "react";

import "@808/snapbook-prototype/styles.css";

export default function SnapbookPreviewLayout({ children }: Readonly<{ children: ReactNode }>) {
  return children;
}
